{% extends 'layout.html.twig' %}

{% block title %}Wild Zoo | Nos animaux{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('animals') }}
{% endblock %}

{% block body %}
    <main class="container-fluid animals">
        <h1 class="my-2">Nos animaux</h1>
        <div class="row">
            {% for animal in animals %}
                {% include 'animal/_card.html.twig' %}
            {% endfor %}
        </div>
        <a class="btn btn-outline-secondary" href="{{ path('home') }}">Retour</a>
    </main>
{% endblock %}
