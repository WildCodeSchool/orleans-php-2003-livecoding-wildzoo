{% extends 'layout.html.twig' %}

{% block title %}WildZoo | Accueil{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('home') }}
{% endblock %}

{% block header %}
    {{ parent() }}
    {% include 'home/_header.html.twig' %}
{% endblock %}

{% block body %}
    <section class="container-fluid">
        <h2>Nos animaux</h2>
        <div class="row">
        {% for animal in animals %}
            <div class="col-md-4 col-sm-6">
                <figure class="card">
                    <figcaption class="card-body">
                        <h3>{{ animal.name }}</h3>
                        <a href="{{ path('animal_show', {'id': animal.id}) }}" class="btn-outline-primary">Voir plus</a>
                    </figcaption>
                </figure>
            </div>
        {% endfor %}
        </div>
    </section>

{% endblock %}
